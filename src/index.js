import React from 'react';
import ReactDOM from 'react-dom/client';
import CProgrammingSmartboard from './components/CProgrammingLoops/CProgrammingLoops';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CProgrammingSmartboard />
  </React.StrictMode>
);