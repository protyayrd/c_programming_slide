import React from 'react';
import ReactDOM from 'react-dom/client';
import CProgrammingLoops from './components/CProgrammingLoops/CProgrammingLoops';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CProgrammingLoops />
  </React.StrictMode>
);